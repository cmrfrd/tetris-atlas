## conclusion

VERDICT: YES — cite Temprano (arXiv:1506.07204) as establishing that OFFLINE-HD board-CLEARING (P1, clear variant) is NP-hard in OUR exact skyline-hard-drop no-kick model, and NP-complete once we supply the routine (unstated in the paper) membership argument. (Q1) His model is functionally EXACTLY ours on his instances. Full rule-difference list: (i) lateral placement is physical top-row traversal vs our teleport-to-column — neutralized by his 5 spare rows above the 16+4T walls (text on p2 and Lemma 3's premise force walls of height 16+4T despite figures drawing them to the top); his soundness lemmas enumerate ALL (rotation,column) straight drops, which IS our full action space, so our being weakly more permissive in general cannot help a cheater, and the intended play trivially embeds — both reduction directions transfer verbatim; (ii) lock timing: contact-lock with no mid-fall input = identical outcome to instant hard drop; (iii) clear semantics: naive full-row clear with rigid downward shift, no cascade — explicitly stated on p8 ('it will not fall otherwise, even if there are gaps that are not filled under it'), identical to the tetris-atlas engine; (iv) loss rule: 'blocks reach the top' vs our post-clear height>m — never engaged (intended play tops at 16+4T, 5 below ceiling; all deviations are fatal for the CLEARING objective by counting, not by loss, so our more lenient transient-poke rule changes nothing); (v) objective is clear-the-board, not survival; (vi) fully offline explicit sequence, no bag/hold/preview, only 5 of 7 piece types (O and Z never appear). (Q2) The proof is SOUND at the architecture level and I independently re-verified every counting argument: global cell count walls+garbage+supply = (16+4T)(4s+3) is EXACT; per-bucket equation sigma+k=T+3 with B/4<a_i<B/2 forces k=3, sigma=T (k=1,2,4,5 all contradict); and I reconstructed a fully airtight version of Lemmas 1-4: rows never merge under naive clears, walls keep all 16+4T rows nonempty, each clear consumes exactly one fill-column cell, and ONLY vertical I (sole width-1 shape) can ever reach the 1-wide fill column — any non-I cell over it floats above 16+4T making the column permanently unreachable (zero clears). Hence board cleared iff exactly 16+4T clears iff all 4+T I's go vertically into the fill column iff every other column is exactly full at the I-phase — every cheat class I probed (material above line, covered gaps, I-only 1-wide slots, I misuse in buckets, horizontal-I straddling the fill column, post-clear wall-shrink exploits in the I-phase, splitting a number across buckets, reopening the same bucket, leaving a bucket open when an LG arrives, cleanup-LG misallocation) is provably fatal by counting plus the lemma-forced single-open-bucket state machine. CAVEATS for citing: (1) non-peer-reviewed solo preprint; Theorem 2 claims only NP-hardness, NP membership must be argued by us (easy: strong NP-completeness of 3-PARTITION gives unary numbers; board (4s+3)x(16+4B+5); sequence 3sB+10s+4+B pieces; placement list is a poly certificate); (2) Lemmas 5-10 are raster-figure case bashes — panel cardinalities are consistent with the action space (6/6/3/6 and 12) and the surrounding logic is forced, but the per-panel gap claims are not machine-verified; recommend a cheap engine-based verification (enumerate all placements on the ~6 gadget surfaces plus one end-to-end small instance) before treating this as a load-bearing anchor; (3) result requires GROWING width n=4s+3 — says nothing about fixed n=10; (4) does NOT transfer to P1-SURVIVE (his instances trivially survivable), P1'-BAG (sequence grossly bag-illegal: long T,T,RG runs, 4+B consecutive I's, O/Z absent), or P2/P3/P4 (nothing on adaptivity, bag adversaries, infinite play, or empty-board start). The extraction under test is essentially correct end to end, including its non-transfer analysis and its identified weak points; I found one minor arithmetic slip and two nuances (see errors_found).

## errors_found

(1) Piece-count formula in the extraction's NP-membership parenthetical undercounts close pieces: it writes '3s + 3sB + 3s + s + (4+B)' but each of the 3s numbers contributes TWO close pieces (RG+LS), so the third term should be 6s, total 3sB+10s+4+B. Cosmetic — polynomiality and all conclusions unaffected; the extraction's CELL arithmetic elsewhere correctly counts closes as 24 cells (3x8) per bucket. (2) Nuance: the extraction says the digit chain has a 'unique gap-free' placement per piece; per the paper (p11) the second T actually has TWO gap-free panels (3rd and 6th), disambiguated only by RG lookahead (the 6th-way surface admits no gap-free RG anywhere). The CHAIN is unique, but the forcing is two-step, resting on gap-permanence (Lemma 4) — worth stating precisely if cited. (3) Nuance/paper ambiguity the extraction resolved silently: Figures 1-5 draw walls to the very top of the board, contradicting the required 16+4T wall height; the text (p2 'five extra lines to decide the movements') and Lemma 3's premise (pieces CAN land above the 16+4T line) force the extraction's reading — correct, but it is an inference, not a literal figure match. (4) Unverified-by-me claim in the extraction: 'cited approvingly by FUN 2024' — I did not re-verify the citation-context claims (no web access used); treat as unconfirmed. (5) The paper's own Lemma 1 prose ('clear a line extra to unlock it') is weaker than what is needed; the actual airtight argument is fill-column unreachability + the exactly-16+4T-clears bound — the theorem survives, but a citation should lean on the counting argument, not Lemma 1's wording. Otherwise: NO soundness-breaking cheat found; the extraction's gadget descriptions, exact-supply arithmetic, sigma+k=T+3 subset-cheat kill, no-early-clears gate, cavity permanence, bucket independence, and all four non-transfer verdicts (P1-SURVIVE, P1'-BAG, P2/P3/P4) check out against the PDF.

## repairs

(1) Piece-count: replace 3s with 6s for close pieces (total 3sB+10s+4+B) — trivial edit, nothing downstream changes. (2) Digit-forcing nuance: rephrase as 'unique gap-free CHAIN, with the T2 branch closed by one-step RG lookahead under gap-permanence' — trivial edit. (3) Wall-height ambiguity: note explicitly that walls=16+4T is inferred from text+Lemma 3, contradicting the stylized figures; if we machine-verify, encode walls at 16+4T. (4) Drop or independently confirm the FUN 2024 citation-context sentence before external use. (5) Residual figure risk (the only substantive one): repair by machine verification with crates/tetris-game — reconstruct the closed surface (3-wide well, garbage = right gun flat + left snake on top per Fig 1), enumerate all (rotation,column) drops for each of LG/RG/T/LS on each of the ~6 reachable gadget surfaces, confirm the gap-free sets match Lemmas 5-10 panel-for-panel, and model-check one small instance (e.g. the paper's {4,3,2,1,1,1,2,2,2}, s=3, B=6... note the paper's example multiset sums 18=3x6 with B=6 but violates B/4<a_i<B/2 — it is illustrative only, use a strict instance) end to end. Estimated: an afternoon with the existing engine. Nothing dies: no unrepairable error was found; the citation claim for P1-CLEAR stands as stated with the listed caveats, and the non-transfer claims (survival, bag-legal, online, Atlas membership) must remain explicitly OPEN.

## confidence

high

