## statements

TARGET THEOREM (P1', proposed): OFFLINE-HD-BAG-CLEAR — "given (n, m, prefilled board b, 7-bag-legal piece sequence w, target R in unary), can the player, using only skyline hard drops (rotation+column chosen pre-drop, no kicks/tucks/slides, cavity entry impossible, post-clear loss check), clear at least R rows?" — is NP-complete. Reduction from 3-PARTITION (strongly NP-complete: items a_1..a_{3s}, sum sB, B/4 < a_i < B/2; optionally M-scaled a'_i = M·a_i, B' = MB, M = poly hardening constant). Membership in NP is immediate (placement list is a poly certificate; simulation poly). SURVIVAL corollary (P1'-SURVIVE): same board with m chosen so the intended zero-waste play fits exactly and w extended by a bag-legal flood that overflows any wasted-cell deviation — small delta, stated not built.

STATUS DELIVERED: a CONCRETE gadget family with machine-verified geometric primitives (new ones this session, in python skyline simulator matching the engine semantics verified at source level in the prior session), full layout/routing/budget arithmetic, a completeness play, and 8 numbered soundness obligations of which TWO (O2 boundary-closure forcing, O3 terminal-slack patching) are genuinely open and one (O2) is the crux. This is a PARTIAL construction: the fractional-item-splitting obstruction that killed three simpler designs is precisely localized, and the delivered ratchet mechanism (S+Z per-bag unit cycle) reduces it to a finite-flow-invariant claim — but I could not close it in-budget and do not claim the theorem.

NEW MACHINE-VERIFIED PRIMITIVES (python skyline simulator, /Users/cmrfrd/.claude/jobs/2257e482/tmp/{ratchet,dev,states,final}.py; semantics = pure column-heights drop, flush = zero new holes, identical to tetris.rs apply_piece_placement semantics verified in the prior session):
- MV7 (RATCHET): width-4 profile A=(0,1,2,1)+h: S has EXACTLY ONE flush placement (vertical S at cols 2,3) → B=(0,1,4,3)+h; on B, Z has EXACTLY ONE flush placement (vertical Z at cols 0,1) → A+(2,2,2,2). One bag's S+Z pair = one self-re-arming "unit" adding exactly 2 uniform rows. No strict-hostility version exists (exhaustive: 0 profiles ≤ offset 6 give the fully-hostile cycle); residual flush leaks on A: {T:2 placements, Z:2, I:4}; on B: {S:1, T:2, I:4}.
- MV8 (OPS CHAIN): close = I@col0: A-family → C1=(3,0,1,0)-family; re-arm = I@col1, I@col2, I@col3 → A+(4,4,4,4) uniform, through C2=(3,4,1,0), C3=(3,4,5,0). C3 is S-hostile; C1, C2 each leak exactly one S-flush and one Z-flush into stray families. All four columns advance equally per close+arm cycle (op cells uniform: +4/column) — this makes per-bucket column bookkeeping a one-parameter linear family (load-bearing for the budget algebra).
- MV9 (TERMINAL FIX): from C1-family: T(nub-left)@col2 then L@col1 → flat +3, unique-genre 2-piece fixes {(L,T),(J,T)}; from A-family: T then L → flat +3.
- MV10 (ANVIL): T-down on 3-column profile (a, a−1, a) → (a+1,a+1,a+1) flush — finishes the width-2 S/Z staircase wells (which are parity-blocked from ever ending flush by themselves: any width-2-confined tetromino changes the column-height difference by an even number, and S/Z-friendliness requires odd diff ±1 — a small new parity theorem, argued, easily kernel-checkable).
- MV11 (T-WELL LEAK AUDIT): all 4 four-T flush fillings of the 4×4 T-well period found; every intermediate pinwheel profile admits foreign flush placements (S:≤2, Z:≤3, I, O, L, J) — T-well is NOT type-pure; only the width-1 I-well and width-1 gate are provably type-pure (any other piece spans ≥2 columns and rests on full-height wall tops = waste).

## proofs

(1) GLOBAL LAYOUT (left→right, every region separated by 1-wide prefilled walls of height exactly R; n = 5s + 35 + O(1); m = 2R; R = 4·(U + 12s + s + 2 + pads) where U = sMB = total unit bags; all poly since 3-PARTITION is strongly NP-complete). [COMPLETE arithmetic, machine-checked in final.py: prefill = n·R − 4·(#below-line pieces) ≥ 0, all counts consistent; e.g. s=2, B=10, a=(3,3,4,4,3,3): U=20, phase=24, fix=4, R=192, N=96 bags, n=41, prefill=6336.]
 - GATE: 1 empty column; filled by the last R/4 bags' I's; no row is ever full before gate I's arrive (gate empty ⇒ no clear ⇒ Temprano-style no-clear discipline); each gate I completes 4 rows which clear immediately (legal under our post-clear loss rule); R/4 gate I's ⇒ exactly R rows cleared. Width-1 ⇒ type-pure (only vertical I ever lands below R).
 - s BUCKETS: width 4, prefilled to C1-family (d+3, d, d+1, d), d = R − 15 − 2B' − fix-offsets (exact value fixed by MV9 fix choice; positive by choice of R). Lifecycle per bucket: [arm: 3 I's → A-family] → absorb units → [close: 1 I → C1-family], repeated exactly 3 times (uniform, assignment-independent), then terminal fix bag (T+L → flat R). Per bucket: exactly 12 op-I's + 2B' unit-rows + 1 T + 1 L; every column receives identical op-cells (MV8 uniformity) ⇒ final flat R ⟺ units absorbed = exactly B'.
 - LOBBY: width-2 S-staircase (MV2) + 1 anvil column prefilled to R−1; absorbs the S of every non-unit bag (12s phase + fix + gate + pad bags = N − U of them, exact capacity); terminal T-down (MV10) → flush R.
 - Z-WELL: mirror staircase + anvil; absorbs every non-unit bag's Z; terminal T-down.
 - I-WELL: width-1 shaft, type-pure; absorbs unit-bag and fix-bag I's (U + fix count, exact; prefill pads residue mod 4).
 - O-WELL: width 2 flat, absorbs every O (N of them, 2 rows each; R = 4·core makes prefill = 0 work exactly).
 - T-WELL: width 4 pinwheel (MV3/MV11), absorbs every T except s bucket-fixes + 2 anvil T-downs; count padded ≡ 0 mod 4 by junk-only pad bags.
 - L-WELL, J-WELL: width 4, MV5 pairs, counts padded even; L-well short s L's (bucket fixes) by design.
(2) PER-BAG ROUTING TABLE. UNIT BAG (item i, a'_i of them, order S before Z): S → armed bucket (unique flush placement, MV7), Z → same bucket (unique flush placement on B), I → I-well, O/T/L/J → wells. PHASE BAGS (4 per item boundary: 1 close + 3 arm; 3 for the first boundary, 1 final close; total 12s exactly = s·12 bucket op-I's, an exact match, assignment-independent): I → bucket op (close old / arm new — the player's ∃-choice of bucket is WHICH closed bucket the 3 arm-I's enter), S → lobby, Z → Z-well, others → wells. FIX BAGS (s+2): T (+L) → designated bucket / anvil T-down; I → I-well; rest → wells. GATE BAGS (last R/4): I → gate (order: I last within bag); the 6 junk pieces land above the completed line (designated slack — see O3). PAD BAGS: everything → wells.
(3) BUDGET ALGEBRA [COMPLETE, machine-checked]: every non-gate-junk piece has a below-line home; below-line capacity n·R = total prefill + 4·(N·7 − 6·R/4); each region's final height is exactly R with zero holes under intended play; since the gate blocks all clears until the terminal phase, any hole below the line or any non-designated cell parked at row ≥ R leaves some row incomplete ⇒ fewer than R rows clear ⇒ NO. Per-bucket linear family: column intake_c = 2·U_j + 4·I_c with MV8 uniformity forcing all four columns to share one slack parameter v_j: I-count(bucket j) = 12 + 4v_j, U_j = B' − 2v_j; Σ_j U_j = sMB (all units must land in buckets: unit-bag S's have no other home when lobby/wells are exact) and ΣI conservation force Σv_j = 0.
(4) COMPLETENESS [ARGUED, simulation-grade; all local steps machine-verified]: given a 3-partition {P_1..P_s}, serve items in input order; at each boundary close the old bucket and arm the bucket assigned to the next item; each bucket is armed exactly 3 times, absorbs exactly B' units in ≤3 intervals, ends C1-family at exact height; terminal fixes flatten everything to R; gate I's clear exactly R rows. Every placement in this play is one of MV2/MV3/MV5/MV6/MV7/MV8/MV9/MV10 — all flush, all skyline-legal, w is bag-legal by construction (each bag = one permutation, designer-ordered).
(5) SOUNDNESS ARCHITECTURE [PARTIAL]: zero-waste is forced (budget, (3)); zero-waste play must route every piece to a flush placement; the ratchet serializes unit absorption to the unique armed bucket at 1 unit/bag; per-bucket exactness + M-scaling pin bucket totals to B' up to 2v_j with |v_j| bounded by op-count feasibility; extraction of a 3-partition follows IF (O2) buckets are provably closed across item boundaries and at most one bucket is armed at any time.

## gaps

NUMBERED OPEN OBLIGATIONS (each with the finite enumeration or argument needed):
O1 (ratchet leak closure) — C1/C2 accept one stray S/Z each; A accepts 2 T's and 2 strays Z; B accepts 1 stray S; T/L/J/O-well intermediates accept foreign pieces (MV11). NEEDED: bounded deviation-graph enumeration (dev.py genre, done to depth 6 — most strays RETURN to the track within 6 plies using extra pieces) upgraded to a piece-COST-annotated closure: show every stray-and-return path consumes ≥1 piece whose global count is exact, i.e., reduce all leaks to flow accounting. Machine-checkable; MODERATE.
O2 (THE CRUX — boundary closure / anti-spanning serialization): nothing yet FORCES the player to close a bucket at an item boundary; a bucket left armed across a boundary absorbs units of two different items (item splitting ⇒ fractional 3-PARTITION ⇒ unsound). My exact-count net (12s phase I's = 12 op-I's/bucket, lobby exact, v_j-algebra) makes every deviation COST-VISIBLE but I could NOT prove no zero-sum global rerouting implements a split: spanning SAVES 4 I's per skipped boundary and the v-algebra admits compensations (bucket with 4 fewer I's absorbs 2 extra units, all heights exact). Explored and REJECTED repairs, with reasons: (i) width-2 S-buckets with I open/close — diff toggling is height-neutral, all buckets concurrently openable; (ii) parity/mod-4 polarity alternation via T — T-counts become assignment-dependent; (iii) M-scaling alone — splits at unit granularity evade congruence pinning; (iv) per-item private lanes/meters — unchosen meters unfillable (supply deficit factor s). MOST PROMISING remaining route: alternate consecutive items between the A-family ratchet and its MIRROR family (Z-led units, arm chain I@c3,c2,c1), so spanning a boundary requires an in-place family conversion whose op-cells are provably NON-uniform across columns, contradicting the one-parameter column family (MV8 uniformity) needed to reach the exact final profile — a finite check (enumerate all ≤k-piece conversions between the two families and show none has uniform column sums) plus the flow invariant. This is the single hardest remaining obligation.
O3 (terminal slack patching): the R/4 gate bags carry 6R/4 junk pieces with no below-line home (structural: wells are full when gate bags arrive); this 6R-cell late slack could patch deficits left by earlier cheating (lobby is S-patchable at any time). NEEDED: either an argument that patchable deficits cannot encode a split (deficit shapes vs. slack types mismatch), or a gate redesign shrinking slack to O(1) — note the tempting "prefill gate except top/bottom 4" designs FAIL (bottom-empty is the only no-clear-preserving orientation and it needs the full R/4 I's; top-empty is an unreachable overhang). OPEN, second-hardest.
O4 (cross-region exclusion) — every piece overlapping a wall column rests at height ≥ R = waste: PROVEN-trivial given walls prefilled to R (skyline landing = max over footprint columns).
O5 (clear-cascade correctness): the terminal phase's progressive 4-row clears + shifts behave as claimed in the real engine (transient poke legality, garbage shift). NEEDED: one end-to-end engine simulation of a small instance (s=1 toy). Mechanical.
O6 (completeness end-to-end): full simulation of (4) for a concrete YES instance in tetris-game; all local steps already machine-verified; assembly not yet run. Mechanical.
O7 (survival variant): waste-budget flood argument not built.
O8 (pad/parity bookkeeping): mod-4/mod-2 residues of T/L/J/O-well counts and I-well height vs pad-bag insertion — pure arithmetic, machine-checkable, not yet discharged in full generality (checked on one concrete instance).
ASSUMPTIONS: generalized (n,m) with unary-encoded dimensions; objective "clear ≥ R rows" (cumulative); designer controls within-bag order; adversary is nature-free (offline). No claim about P2/P3/P4 is made here.

## confidence_notes

HIGH confidence: all MACHINE-VERIFIED items (MV7-MV11; scripts under /Users/cmrfrd/.claude/jobs/2257e482/tmp/, python simulator whose drop rule is the same column-heights skyline rule verified at source in crates/tetris-game/src/tetris.rs); the budget arithmetic; the parity theorem blocking width-2 flush endings; the impossibility findings (no strict-hostility ratchet at offsets ≤6; meters/lanes supply deficit; gate prefill orientations). MEDIUM: completeness play (assembled from verified local steps, not yet run end-to-end in the engine). LOW / genuinely delicate: soundness as a whole — the design's forcing is COUNT-based, not absolute-hostility-based, and the two open obligations O2 (item-splitting via boundary spanning) and O3 (gate-bag slack patching) are exactly where a referee would attack; three earlier design iterations in this session died at O2's analogue, and I judge the mirror-alternation repair plausible (~60%) but unproven. The honest verdict on the ranking's item 2 ('pure gadget engineering, no new theory') stands CORRECTED: P1' needs one real idea — a skyline-compatible serialization primitive whose absence is now precisely characterized (skyline column-independence makes 'openness' a locally cheap, reversible, concurrency-friendly property; every forcing must therefore be routed through exact global piece-type flows, and the residual freedom in those flows is the entire difficulty). If O2's finite mirror-conversion check succeeds, the rest is enumeration of the same genre as MV1-MV11.

