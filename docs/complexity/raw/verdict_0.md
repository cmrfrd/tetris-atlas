## conclusion

The proof suite is essentially SOUND. All six load-bearing results hold as stated: (a) P1/P1' ∈ NP with correct certificate accounting; (b) P2 ∈ PSPACE with correct per-frame space accounting (depth N, O(nm+log n+log N) bits/frame, N unary ⇒ O(|input|²)); (c) P3 ∈ EXPTIME with a from-scratch, correct attractor construction — C1a/C1b/C1c inductions check out (player nodes enter A only when ALL successors in A; adversary rank-decreasing τ defeats arbitrary randomized history-dependent play), C2's two-sided gfp argument (W post-fixed; every post-fixed point disjoint from every A_i by rank induction) is airtight, and the counter-algorithm complexity is right; (d) the adversarial ⟺ almost-sure ⟺ sure equivalence is CORRECTLY handled at exactly the subtle point flagged: the proof does NOT claim a.s. death from A (which would be false — adversary nodes in A need only one A-successor, so uniform draws can leak into W), proves only death-probability ≥ 7^{−K} under every strategy via a sound rank induction (player nodes in A have ALL successors lower-rank, so mixing can't help; adversary nodes hit the τ-draw w.p. ≥ 1/7), and replaces the folklore Borel–Cantelli one-liner with the correct dichotomy D3 (P(visit A i.o. ∧ survive) = 0 via K-separated stopping times and the uniform-over-histories conditional bound — the geometric iteration is valid because the 7^{−K} bound holds for EVERY history ending in A); memoryless-deterministic-suffices is proven, not assumed (C1b gives sure survival from W, so randomization/memory can add nothing). (e) is honest and its structural claims (APSPACE=EXPTIME kills the naive-gfp route; no hardness transfer from the Demaine-et-al. slide/spin model) are correct. (f)'s encoding arithmetic (2-EXPTIME binary / EXPTIME unary / O(1) fixed) is correct. Engine cross-check: the stated ruleset matches crates/tetris-game/src/tetris.rs exactly (skyline drop from column heights only, lines 2196-2225; clear THEN loss-check, lines 2296-2297 with is_lost = height > ROWS at 2134-2135; 32-bit columns provide the scratch headroom). Six errors found, all minor/peripheral and all repairable; no theorem dies. The one factual error runs in the proofs' FAVOR: the engine's placement count is 162 (verified by width recomputation: 9+17+17+17+34+34+34), matching the task statement — the proof's gap #4 wrongly sides with CLAUDE.md's stale 117 while claiming engine verification.

## errors_found

E1 [FACTUAL, in self-declared gap #4]: The claim "CLAUDE.md and the engine's precomputed table say 117 (engine confirmed... tetris.rs:760-828)" is false. Verified: crates/tetris-game/src/tetris.rs builds NUM_PLACEMENTS from num_rotations (O=1, I/S/Z=2, T/L/J=4; lines 33, 367, 760-828) and widths, giving 162 at n=10 — the task statement's 162 is the engine's number; CLAUDE.md's 117 is stale documentation. The proof asserts an engine verification it gets backwards. Harmless to all theorems (only "≤4n actions/piece" is used), but the verification claim itself is wrong. E2 [LOGICAL, Statement E part 1]: internal contradiction. The parenthetical retraction "NO — that reasoning is wrong...: deciding requires succinct certificates for yes-instances AND for no-instances" is itself incorrect and contradicts the CONDITIONAL STATEMENT two sentences earlier. Because the circuit-invariant check is unconditionally SOUND (any C passing "C(s₀) ∧ ∀s[C(s)⇒s≠⊥ ∧ ∃a∀q C(succ)]" defines a post-fixed point of F, hence S_C ⊆ W by Lemma C2), yes-side succinctness ALONE decides P3: accept iff ∃ poly-size C passing the check — no-instances pass no check by soundness, so this is a Σ-level algorithm ⊆ PSPACE with one-sided certificates only. The retraction wrongly weakens a correct claim. E3 [LOOSE BOUND, Statement E part 1]: the check is placed in Π₂^p and the overall procedure in Σ₃^p, but ∃a ranges over ≤4n explicitly enumerable actions and ∀q over ≤7 draws — both eliminable deterministically — so the check is coNP and guess-C gives Σ₂^p. Stated classes are valid but needlessly weak (not unsound). E4 [UNDERSPECIFIED ALGORITHM, Theorem B closing remark, explicitly non-load-bearing]: "if N were given in binary, survive-min(N,K)... makes P2 decidable in EXPTIME" — running Theorem B's TREE DFS to depth min(N,K) with K = 2^{Θ(nm)} costs (28n)^{2^{Θ(nm)}} = doubly exponential. EXPTIME requires bounded-horizon value iteration memoized over the 2^{O(nm)} arena graph (|V|·horizon = 2^{O(nm)}). The conclusion (binary-N P2 ∈ EXPTIME) is true; the sketched route as literally written is not. E5 [TYPO, Theorem C arena]: adversary edge written "to (b', …, q)" — the board does not change on a piece draw; should be (b, B_d∖{q}, q). All counting, lemmas, and the algorithm use the correct structure; pure notation slip. E6 [COSMETIC INCONSISTENCY, SETUP vs usage]: SETUP declares the bag state "a nonempty subset", but the arena (V_P with "∅ allowed, ≡ pre-refill") and Theorem F's initial position (∅board, ∅bag) use B=∅. The refill convention makes the semantics unambiguous; only the SETUP wording is wrong. NOT errors (attack targets checked and passed): certificate sizes in A; space accounting in B (including the per-frame O(nm) scratch and unary-N argument); C1/C2/C3 determinacy and gfp inductions (no handwaving — both directions of C2 fully proven); D's probability model (Ionescu–Tulcea, cylinder conditioning), Direction 2's rank induction under randomized history-dependent π, the explicit refusal to claim a.s. death from A (correct — the naive Borel–Cantelli argument is invalid there and the proof says so), D3's K-separated stopping-time iteration (valid: the δ bound is uniform over all histories ending in A), memoryless-suffices (derived from C1b, not asserted); F's V_A-position ⟺ 7-fold-conjunction equivalence and encoding arithmetic; the monotonicity-in-m remark (true, and flagged as sketch).

## repairs

All six errors are repairable with local edits; nothing dies. E1: rewrite gap #4 to "the engine's ALL_PLACEMENTS count IS 162 (O=1/I,S,Z=2/T,L,J=4 rotations × column counts: 9+3·17+3·34), matching the task statement; CLAUDE.md's 117 is stale" — one-sentence fix, no proof touched. E2: delete the parenthetical retraction ("...NO — that reasoning is wrong... AND for no-instances...") and keep the preceding CONDITIONAL STATEMENT, optionally adding the observation that soundness of inductive-invariant certificates makes one-sided (yes-side) succinctness sufficient for P3 ∈ Σ₂^p ⊆ PSPACE; the no-side ranked-strategy discussion remains as an independent dual route, not a requirement. E3: replace Π₂^p → coNP and Σ₃^p → Σ₂^p (strict improvement, proof already contains the "constant width 7" observation; just add "≤4n actions are enumerable"). E4: replace the remark's algorithm with "bounded-horizon backward induction memoized over the arena graph, horizon min(N,|V|), time 2^{O(nm)}" — or delete the remark (explicitly non-load-bearing). E5: fix the typo to (b, B_d∖{q}, q). E6: change SETUP to "B ⊆ the 7 types, with B=∅ meaning pre-refill (draw set = full bag)". Post-repair status: Theorems A, B, C, D, F fully correct; Statement E internally consistent and slightly stronger (Σ₂^p conditional). The suite's handling of the two deliberately-set traps — the adversarial⟺almost-sure equivalence (correctly reduced to "positive death probability from A" + the D3 dichotomy, with the false "a.s. death from A" explicitly disclaimed and explained) and memoryless-sufficiency (derived from positional determinacy proven from scratch) — is correct as written and needs no repair.

## confidence

high

