# project files
# Ignore Rust and build artifacts
target/
build/
trace/
traces/
tensorboard/src/tensorboard_generated/
junk/

# vendored dependencies
vendor/
!vendor/.gitkeep

# runtime artifacts (checkpoints, logs, data, output, databases)
artifacts/
!artifacts/.gitkeep